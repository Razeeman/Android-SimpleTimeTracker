<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/containerNotificationTag"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_weight="1"
    android:padding="1dp"
    tools:layout_height="@dimen/notification_controls_size"
    tools:layout_width="100dp">

    <ImageView
        android:id="@+id/ivNotificationTag"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/bg_rounded_tag"
        tools:ignore="ContentDescription"
        tools:tint="@color/black" />

    <TextView
        android:id="@+id/tvNotificationTag"
        style="@style/TextAppearance.Compat.Notification.Info"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:paddingHorizontal="2dp"
        android:textColor="@color/white"
        android:textSize="11sp"
        android:textStyle="bold"
        tools:text="Some really long tag name" />

    <ImageView
        android:id="@+id/ivNotificationTagSelectionCheck"
        android:layout_width="@dimen/notification_icon_half_size"
        android:layout_height="@dimen/notification_icon_half_size"
        android:layout_alignParentTop="true"
        android:layout_alignParentEnd="true"
        android:visibility="gone"
        tools:background="@drawable/spinner_check_mark"
        tools:ignore="ContentDescription"
        tools:visibility="visible" />

    <Button
        android:id="@+id/btnNotificationTag"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_widget_button" />

</RelativeLayout>